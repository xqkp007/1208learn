数据库表整理计划（待确认）

目标
- 统一输出系统内所有涉及的数据库表：表用途说明 + 标准建表 SQL（含字段/表注释、索引、外键）+ 最终表关联图（Mermaid ER）。

范围判定（我将按以下来源梳理）
- `backend/app/models`：SQLAlchemy 模型为主事实来源。
- `backend/sql`：已有建表脚本作为补充/校验。
- `prd/*.md`：用于补足遗漏字段或约束。

已确认事项
- 目标数据库为 MySQL（沿用现有语法与风格）。
- 外部数据源表 `people_customer_dialog` 仅列出并说明关系，不提供建表 DDL。
- 交付物仅为 `数据库表.md`（会覆盖原内容，统一为最终版本）。

执行步骤（你确认后我才开始）
1) 梳理并输出最终表清单与关系草图，与你确认无遗漏。
2) 为每张表生成标准 DDL（统一风格）+ 表用途说明（1-2 句话）。
3) 生成 Mermaid ER 表关联图。
4) 整合为 `数据库表.md`（结构：表清单 → 逐表说明+DDL → ER 图）。

交付物
- `数据库表.md`（最终文档）
